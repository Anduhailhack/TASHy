<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />-->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.5.0/axios.min.js"></script>
    <script src="https://telegram.org/js/telegram-web-app.js?1"></script>
    <script>
        function setTheme() {
            document.documentElement.className = Telegram.WebApp.colorScheme;
        }

        Telegram.WebApp.onEvent("themeChanged", setTheme)
        setTheme()
    </script>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/request.css">
    <title>Request</title>
</head>

<body>
    <ul class="container">
        <div class="questionier">
            <div class="info">
                <% if (student) { %>
                    <% if (student.f_name && student.l_name) { %>
                        <li><%= student.f_name %> <%= student.l_name %></li>
                    <% } %>
                    <% if (student.stud_id) { %>
                        <li><%= student.stud_id %></li>
                    <% } %>
                    <% if (student.email) { %>
                        <li><%= student.email %></li>
                    <% } %>
                    <% if (student.phone_no) { %>
                        <li><%= student.phone_no %></li>
                    <% } %>
                <% } %>
            </div>
            <div class="info">
                <% if (data) { %>
                    <% if (data.diagnosis) { %>
                        <% if (data.diagnosis.code1) { %>
                            <li><%= "The patient has suicidal ideations." %></li>
                        <% } %>
                        <% if (data.diagnosis.code2) { %>
                            <li><%= "The patient has homicidal ideations." %></li>
                        <% } %>
                        <% if (data.diagnosis.code3) { %>
                            <li><%= "The patient has expansive moods and feeling of depression or loneliness."%></li>
                        <% } %>
                        <% if (data.diagnosis.code4) { %>
                            <li><%= "The patient has feelings of lowmood, hopelessness or worthlessness." %></li>
                        <% } %>
                        <% if (data.diagnosis.code5) { %>
                            <li><%= "The patient has symptoms of alcohol or substance withdrawal symptoms" %> </li>
                        <% } %>
                        <% if (data.diagnosis.code6) { %>
                            <li><%= "The patient is experiencing insomnia." %></li>
                        <% } %>
                    <% } else { %>
                        <%= "No diagnosis info was found." %>
                    <% } %>
                <% } else { %>
                    <%= "No diagnosis info was found." %>
                <% } %>
            </div>
            <div class="action">
                <button id="forward">Forward <i class="fa fa-share" aria-hidden="true"></i></button>
                <button id="appoint">Appoint <i class="fa fa-calendar-plus-o" aria-hidden="true"></i></button>
                <button id="reject" class="btn-danger">Reject <i class="fa fa-trash" aria-hidden="true"></i></button>
            </div>
        </div>
    </ul>

    <script type="text/javascript" src="/js/script.js"></script>
    <script type="text/javascript" src="/js/admin/request.js"></script>
</body>

</html>